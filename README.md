# skills

Monorepo for OpenClaw skills.

## Included

- `skills/12306-train` — 12306 direct/transfer ticket query + prices + route (stops)
- `skills/amap-spaces` — AMap (Gaode) Web Service API CLI (`amap`) for POI/geocode/route

## Notes

- API keys are read from environment variables or local OpenClaw config, and are **not** committed.
